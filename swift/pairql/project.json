{
  "root": "pairql",
  "projectType": "library",
  "implicitDependencies": ["gertie"],
  "targets": {
    "build": {
      "command": "cd pairql && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    }
  }
}
