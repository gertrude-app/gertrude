{
  "root": "gertie-tca-features",
  "projectType": "library",
  "implicitDependencies": ["gertie", "pairql", "pairql-ios-apps"],
  "targets": {
    "build": {
      "command": "cd gertie-tca-features && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    },
    "test": {
      "command": "cd gertie-tca-features && SWIFT_DETERMINISTIC_HASHING=1 ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift test"
    }
  }
}
