{
  "root": "pairql-iosapp",
  "projectType": "library",
  "implicitDependencies": ["pairql"],
  "targets": {
    "build": {
      "command": "cd pairql-iosapp && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    }
  }
}
