name: GertiePodcasts
options:
  deploymentTarget:
    iOS: "17.0"
  developmentLanguage: en
  knownRegions:
    - en
    - es
    - Base

configs:
  Debug: debug
  Release: release

targets:
  'Gertrude Podcasts':
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: app/sources
        group: app
      - path: app/Assets.xcassets
        group: app
      - path: app/LaunchScreen.storyboard
        group: app
      - path: app/StoreConfig.storekit
        buildPhase: none
        group: app
    dependencies:
      - package: LibCore
      - package: LibViews
      - package: LibTCA
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1,2" # iPhone and iPad
        SWIFT_VERSION: "6.0"
        PRODUCT_NAME: "Gertrude Podcasts"
        DEVELOPMENT_TEAM: "WFN83LM943"
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: app/Info.plist
        MARKETING_VERSION: 1.6.3
        CURRENT_PROJECT_VERSION: 28
        ALWAYS_SEARCH_USER_PATHS: NO
      configs:
        Release:
          PRODUCT_BUNDLE_IDENTIFIER: "com.netrivet.gertrude.am"
          ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
          API_ENDPOINT: "https://api.gertrude.app"
        Debug:
          ONLY_ACTIVE_ARCH: YES
          PRODUCT_BUNDLE_IDENTIFIER: "com.netrivet.gertrude.am.dev"
          ASSETCATALOG_COMPILER_APPICON_NAME: AppIconDev
          API_ENDPOINT: "https://sweet-carefully-tarpon.ngrok-free.app"
    scheme:
      storeKitConfiguration: "app/StoreConfig.storekit"
    info:
      path: app/Info.plist
      properties:
        CFBundleName: $(PRODUCT_NAME)
        CFBundleDisplayName: Gertrude Podcasts
        CFBundleIdentifier: $(PRODUCT_BUNDLE_IDENTIFIER)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleLocalizations:
          - en
          - es
        API_ENDPOINT: $(API_ENDPOINT)
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
        UIRequiresFullScreen: true
        UILaunchStoryboardName: LaunchScreen
        UIBackgroundModes:
          - audio
          - fetch
          - processing
        BGTaskSchedulerPermittedIdentifiers:
          # NB: keep in sync with keys in RegisterBgTasks.swift
          - com.netrivet.gertrude.am.refresh-feed
          - com.netrivet.gertrude.am.download-episodes

packages:
  LibCore:
    path: ./lib-core
  LibViews:
    path: ./lib-views
  LibTCA:
    path: ./lib-tca
