{
  "root": "iosapp/lib-ios",
  "projectType": "library",
  "implicitDependencies": ["gertie", "pairql-iosapp"],
  "targets": {
    "build": {
      "command": "cd iosapp/lib-ios && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    },
    "test": {
      "command": "cd iosapp/lib-ios && SWIFT_DETERMINISTIC_HASHING=1 ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift test"
    }
  }
}
