{
  "root": "pairql-macapp",
  "projectType": "library",
  "implicitDependencies": ["pairql", "gertie"],
  "targets": {
    "build": {
      "command": "cd pairql-macapp && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    },
    "test": {
      "command": "cd pairql-macapp && SWIFT_DETERMINISTIC_HASHING=1 ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift test"
    }
  }
}
