{
  "root": "duet",
  "projectType": "library",
  "implicitDependencies": ["x-kit"],
  "targets": {
    "build": {
      "command": "cd duet && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    },
    "test": {
      "command": "cd duet && SWIFT_DETERMINISTIC_HASHING=1 ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift test"
    }
  }
}
