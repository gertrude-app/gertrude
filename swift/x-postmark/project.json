{
  "root": "x-postmark",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "cd x-postmark && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    },
    "test": {
      "command": "true"
    }
  }
}
