{
  "root": "gertie",
  "projectType": "library",
  "implicitDependencies": ["x-kit"],
  "targets": {
    "build": {
      "command": "cd gertie && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    },
    "test": {
      "command": "cd gertie && SWIFT_DETERMINISTIC_HASHING=1 ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift test"
    }
  }
}
