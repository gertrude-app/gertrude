# dev-emails

Local SolidStart tool for iterating on Gertrude's transactional email templates (HTML
and CSS) with hot reload. Reads `../index.html` from the working directory and renders
it in a resizable iframe with light/dark toggle for quick visual feedback.

Run with `pnpm dev`.
