{
  "root": "pairql-ios-apps",
  "projectType": "library",
  "implicitDependencies": ["pairql", "gertie"],
  "targets": {
    "build": {
      "command": "cd pairql-ios-apps && ${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/bin/swiftly run swift build"
    }
  }
}
